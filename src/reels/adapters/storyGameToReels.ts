import { storyToMarketingReels } from "../../../functions/_lib/realm-reels.mjs";
export function storyGameToReels(storyGame: unknown, options: unknown) { return storyToMarketingReels(storyGame, options, "story_game"); }
