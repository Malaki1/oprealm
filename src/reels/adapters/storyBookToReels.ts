import { storyToMarketingReels } from "../../../functions/_lib/realm-reels.mjs";
export function storyBookToReels(storyBook: unknown, options: unknown) { return storyToMarketingReels(storyBook, options, "storybook"); }
